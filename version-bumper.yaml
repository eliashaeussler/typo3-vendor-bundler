presets:
  - typo3-commit-guidelines

filesToModify:
  - path: bin/typo3-vendor-bundler
    patterns:
      - "\\$applicationVersion = '{%version%}';"
    reportUnmatched: true

releaseOptions:
  commitMessage: '[RELEASE] Release of typo3-vendor-bundler {%version%}'
  signTag: true
